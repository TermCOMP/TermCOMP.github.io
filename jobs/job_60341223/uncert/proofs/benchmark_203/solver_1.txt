NO
We consider the system wpc8q.

  Alphabet:

    f : [a -> a] --> b 
    g : [a -> a] --> a -> a 

  Rules:

    g(F) X => X 
    f(F) => f(g(F)) 

It is easy to see that this system is non-terminating:

  f(h) 
    => f(g(h)) 

That is, a term s reduces to a term t which instantiates s.

