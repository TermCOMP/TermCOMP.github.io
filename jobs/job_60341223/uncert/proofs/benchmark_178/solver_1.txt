MAYBE
We consider the system TRG2y.

  Alphabet:

    const : [a * b] --> a 
    fix : [(f -> g) -> f -> g] --> f -> g 
    subst : [c -> d -> e * c -> d * c] --> e 

  Rules:

    const(X, Y) => X 
    subst(F, G, X) => F X (G X) 
    fix(F) X => F fix(F) X 

