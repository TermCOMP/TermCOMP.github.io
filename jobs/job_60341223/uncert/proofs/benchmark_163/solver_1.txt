YES
We consider the system 0lJ91.

  Alphabet:

    uncurry : [a -> b -> c * a * b] --> c 

  Rules:

    uncurry(F, X, Y) => F X Y 

Symbol uncurry is an encoding for application that is only used in innocuous ways.  We can simplify the program (without losing non-termination) by removing it.  This gives:

  Alphabet:


  Rules:


