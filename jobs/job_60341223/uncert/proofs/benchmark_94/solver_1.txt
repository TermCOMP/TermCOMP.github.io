MAYBE
We consider the system by6xN.

  Alphabet:

    f : [nat * nat * nat] --> nat 
    g : [nat * nat -> nat] --> nat 
    h : [nat * nat] --> nat 
    s : [nat] --> nat 

  Rules:

    f(X, Y, s(Z)) => g(h(X, Y), /\x.f(x, X, Z)) 
    h(X, X) => f(X, s(X), s(s(X))) 

