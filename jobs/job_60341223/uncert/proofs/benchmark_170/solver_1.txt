YES
We consider the system kOxCT.

  Alphabet:

    apply : [a -> b * a] --> b 

  Rules:

    apply(F, X) => F X 

Symbol apply is an encoding for application that is only used in innocuous ways.  We can simplify the program (without losing non-termination) by removing it.  This gives:

  Alphabet:


  Rules:


